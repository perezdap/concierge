apiVersion: v1
kind: Service
metadata:
  name: concierge-gateway
  labels:
    app.kubernetes.io/name: concierge
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: concierge
    app.kubernetes.io/component: gateway
  ports:
    - name: http
      port: 8765
      targetPort: 8765
      protocol: TCP
